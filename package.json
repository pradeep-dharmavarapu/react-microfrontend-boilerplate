{
  "name": "react-microfrontend-boilerplate",
  "private": true,
  "scripts": {
    "start": "npm run start --workspace=shell",
    "build": "npm run build --workspace=shell",
    "install:all": "npm install --workspace=shell"
  },
  "workspaces": ["shell"]
}
