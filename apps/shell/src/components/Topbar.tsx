export function Topbar() {
  return (
    <header className="topbar">
      <div>
        <p className="eyebrow">Good evening, Pradeep</p>
        <h2>Everything important is already in view.</h2>
      </div>
      <div className="topbar-actions">
        <button className="ghost-button">Sync</button>
        <button className="primary-button">New widget</button>
      </div>
    </header>
  );
}
